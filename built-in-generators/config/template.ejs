// require() your dependencies here


exports.mount = function <%= functionName %> (router) {
  // `router` is an express router.
  // Add to it as you normally would.
  // Example:
  //   router.get('/some-prefix', require('../my-thing/index.js'))
}
